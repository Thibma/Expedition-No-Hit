-- =============================================================================
-- ExpeditionNoHit — User Configuration
-- =============================================================================
-- Edit the values below to customise the mod behaviour.
-- Changes take effect after reloading the mod (or restarting the game).
-- =============================================================================

return {
    -- When true: the retry popup appears immediately at the moment a hit is
    -- detected, skipping the long game over animation entirely.
    -- When false: the standard defeat sequence plays (animation then popup).
    quickGameOver = true,

    -- Enable verbose debug logging in the UE4SS console.
    debug = true,
}
